# 算法

## 二分算法

> 使用这个算法需要注意
>
> - 必须是有序的数组(单调的,升序或者降序)
> - 可以通过中间的元素推断是在左边还是右边
>
> 分治思想
>
> ```JavaScript
> // 伪代码
> let len = arr.length;
> let left = 0
> let right = len - 1
> let mid = left + (right - left) / 2
> 
> if num = mid
> return num
> if
> ```
>
> 